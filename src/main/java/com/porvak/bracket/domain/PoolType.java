package com.porvak.bracket.domain;

public enum PoolType {

    /**
     * Pools that are open to anyone
     */
    PUBLIC,

    /**
     * Pools that are only open to a specific list of users.
     */
    PRIVATE
}
